import { InventorySection } from "../_components/inventory-section";

export const metadata = {
  title: "MediFind | Inventory",
};

export default function InventoryPage() {
  return <InventorySection />;
}
